{
  "name": "job_app",
  "version": "1.0.0",
  "scripts": {
    "start-server": "echo Starting server... && cd server && npm start",
    "start-client": "echo Starting client... && cd client && npm start",
    "start": "concurrently \"npm run start-server\" \"npm run start-client\""
  },
  "devDependencies": {
    "@testing-library/jest-dom": "^6.4.5",
    "@testing-library/react": "^15.0.7",
    "concurrently": "^7.0.0"
  }
}
